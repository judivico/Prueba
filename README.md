# Prueba

Aquí es para ensañar el git 
para clonar usamos git clone enlace
git add archivos
git commit -m "mensaje"
git push         para subir a la nube
git log
git branch      crear rama
git merge 	unir ramas
git status
git checkout nombrerama o nombrecommit       pasar de commit o ramas